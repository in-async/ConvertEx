namespace InAsync.Tests.TestHelpers.Models {

    public enum ByteEnum : byte {
        None = 0,
        Alpha = 1,
        Beta = 2,
    }

    public enum IntEnum : int {
        None = 0,
        Gamma = 3,
        Delta = 4,
    }
}